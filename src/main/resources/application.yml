spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mysqlDb
    username: root
    password: mysqldb
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
